import React from 'react'; // Import React if you're using React JSX syntax

// Define a functional component
const Info: React.FC = () => {
  return (
    <div className="text-center">
      <h1>Info</h1>
      <p>This is a basic TypeScript with JSX setup.</p>
    </div>
  );
};

export default Info; // Export the component
